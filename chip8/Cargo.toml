[package]
name = "chip8"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
js-sys = "0.3"
# We need 'js' feature for getrandom to work in WASM
getrandom = { version = "0.2", features = ["js"] }